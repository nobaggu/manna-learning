#Rag!= vector db 전용
-사실 RAG의 정의는 "검색 방식이 뭐든 상관 없이, 외부 데이터를 끌어와 LLM 답변에 주입하는 패턴.
-즉 필요할 때 외부 지식을 검색해서 llm의 prompt에 넣어주는 구조
-db에서 정보 가져옴-> llm한테 db의 정보를 섞어서 답해달라고 요청-> llm은 db정보 반영하여 대답
#한계
-그러나 코사인 유사도로 가져오는 것이 항상 정답이 아니기에 self-rag 같은 보완책을 쓰는 것
